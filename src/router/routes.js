import Home from '../container/home/Home.jsx';
// import Error from '../container/Error.jsx';
// import Login from '../container/Login.jsx';

const routes = [

    { name: "Home", path: "/", component: Home, exact: true },

    // { name: "Login", path: "/Login", component: Login, auth: true, exact: false },

    // { name: "Error", path: "/Error", component: Error, exact: false },

]

export default routes;